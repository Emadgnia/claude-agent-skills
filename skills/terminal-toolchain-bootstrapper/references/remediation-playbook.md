# Remediation Playbook

1. Confirm binary location (`which <cmd>`).
2. Confirm version (`<cmd> --version`).
3. Confirm PATH and shell profile export.
4. Reinstall/update only when mismatch is confirmed.
5. Validate with original failing command.
