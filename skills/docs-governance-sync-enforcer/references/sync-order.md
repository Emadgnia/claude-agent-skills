# Sync Order

Apply this order when policy and implementation both change:

1. governance policy (`AGENTS.md`)
2. routing (`Skills.md`)
3. architecture record (`Architecture.md`)
4. plan evidence (`plans/`)
5. validation docs (`tests/`, deploy docs)
